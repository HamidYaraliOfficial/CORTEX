<Page
    x:Class="Cortex.UI.Views.ArchitectureCanvasView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Background="{ThemeResource CortexCanvasBackgroundBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar: zoom, focus mode, clustering, filters -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="4" Padding="6">
            <Button x:Uid="Canvas_ZoomIn" Click="OnZoomIn"/>
            <Button x:Uid="Canvas_ZoomOut" Click="OnZoomOut"/>
            <Button x:Uid="Canvas_FitToScreen" Click="OnFitToScreen"/>
            <ToggleButton x:Uid="Canvas_FocusMode" Click="OnToggleFocusMode"/>
            <ComboBox x:Uid="Canvas_EdgeFilter" SelectedIndex="0" MinWidth="160">
                <ComboBoxItem x:Uid="Canvas_EdgeFilter_All"/>
                <ComboBoxItem x:Uid="Canvas_EdgeFilter_DependsOnly"/>
                <ComboBoxItem x:Uid="Canvas_EdgeFilter_CallsOnly"/>
                <ComboBoxItem x:Uid="Canvas_EdgeFilter_InheritanceOnly"/>
            </ComboBox>
        </StackPanel>

        <!-- Pannable / zoomable graph surface -->
        <ScrollViewer Grid.Row="1" x:Name="CanvasScrollViewer"
                      ZoomMode="Enabled" MinZoomFactor="0.1" MaxZoomFactor="4"
                      HorizontalScrollMode="Enabled" VerticalScrollMode="Enabled"
                      HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
            <Canvas x:Name="GraphCanvas" Width="4000" Height="3000" Background="Transparent"/>
        </ScrollViewer>

        <!-- Minimap, bottom-right overlay -->
        <Border Grid.Row="1" HorizontalAlignment="Right" VerticalAlignment="Bottom"
                Width="180" Height="120" Margin="12" CornerRadius="6" Opacity="0.9"
                Background="{ThemeResource CortexNodeFillBrush}" BorderThickness="1"
                BorderBrush="{ThemeResource CortexEdgeStrokeBrush}">
            <Canvas x:Name="MinimapCanvas"/>
        </Border>
    </Grid>
</Page>
