<Page
    x:Class="Cortex.UI.Views.RepositoryExplorerView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid Padding="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <AutoSuggestBox Grid.Row="0" x:Uid="RepoExplorer_SearchBox" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="6" Margin="0,0,0,8">
            <Button x:Uid="RepoExplorer_AddRepository" Style="{StaticResource AccentButtonStyle}"/>
            <Button x:Uid="RepoExplorer_NewWorkspace"/>
        </StackPanel>

        <TreeView Grid.Row="2" x:Name="RepositoryTree" x:Uid="RepoExplorer_Tree"
                  SelectionMode="Single" CanDragItems="False" CanReorderItems="False"/>
    </Grid>
</Page>
